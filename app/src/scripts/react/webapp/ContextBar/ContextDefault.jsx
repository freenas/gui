// OVERVIEW BAR NAVIGATION
// ========================
// This is the default view shown in the FreeNAS Context Bar when no other
// contexts are active.

"use strict";

import React from "react";

const ContextDefault = React.createClass(
  { render () {
      return null;
    }

  }
);

export default ContextDefault;
